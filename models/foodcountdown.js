class foodcountdown {
  constructor(id, title, quantity, initialPrice, price, time) {
    this.id = id;
    this.title = title;
    this.quantity = quantity;
    this.initialPrice = initialPrice;
    this.price = price;
    this.time = time;
  }
}

export default foodcountdown;
