class foodcountdown {
  constructor(id, title, quantity, initialPrice, price, time, image) {
    this.id = id;
    this.title = title;
    this.quantity = quantity;
    this.initialPrice = initialPrice;
    this.price = price;
    this.time = time;
    this.image = image;
  }
}

export default foodcountdown;
